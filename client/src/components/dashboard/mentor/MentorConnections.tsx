import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function MentorConnections() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Connections</CardTitle>
      </CardHeader>
      <CardContent>
        <p>See and manage your connections with founders and investors.</p>
        {/* TODO: List and manage connections here */}
      </CardContent>
    </Card>
  );
}
